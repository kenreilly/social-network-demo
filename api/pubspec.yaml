name: api
description: A web server built using the shelf package.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  core: 
    path: ../core/
  api_sdk:
    path: ../api-sdk/

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.5.1
